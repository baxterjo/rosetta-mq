# Local broker for manually exercising rosetta-mq (see the `just` recipes in the repo root
# Justfile). Anonymous access on the default MQTT port -- not for anything but local dev.
listener 1883
allow_anonymous true
